import React from "react";
import { DicomObject } from "../lib/dicomReader";

interface ImageViewerProps {
    dcmImage: DicomObject;
}

function ImageViewer({dcmImage}: ImageViewerProps) {
    return <></>
}

export default ImageViewer;